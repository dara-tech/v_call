{
  "name": "v_call-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --prefix ../v_server",
    "dev:client": "npm run dev --workspace=client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run build --workspace=client",
    "build:client": "npm run build --workspace=client",
    "install:all": "npm install && npm install --prefix ../v_server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
